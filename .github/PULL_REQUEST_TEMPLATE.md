## Problem

<!-- What tarball is wrong, and how did you see it? (`npm pack --dry-run`, a publish, a consumer install) -->

## Expected

<!-- What packgate should do (finding kind, exit code, docs) -->

## Actual

<!-- Command, output, packgate version or commit -->

## Package shape (redact secrets)

- `package.json` `files` / `.npmignore` / `.gitignore` (relevant parts only)
- Node and npm versions
